# 意图契约 v3 —— 由 v2 更新于 2026-08-08 03:00

## 项目目标

开发 lua_notify：notify crate 的 Lua 5.4 绑定，poll 模式跨平台文件监控。测试用真实临时目录验证。遵循家族模板和 CI 三平台。

## 约束
- 约束在本项目目录的意图契约中维护

## 技术栈入口
- 技术栈入口在本项目目录中维护

## Non-goals（不做）
- 不引入新的基础设施组件（如 Redis、Kafka 等）
- 不修改数据库索引结构

## 验收标准
- 所有任务完成后必须通过自审（verification.md）
- 置信度低于 0.7 的产出必须进入决策流
- 用户可见行为的产出必须声明真实入口验证（ENTRY_LEVEL: real_user）

## v2 变更（pm）
- 项目目标：开发 lua_notify：notify crate 的 Lua 5.4 绑定，poll 模式跨平台文件监控